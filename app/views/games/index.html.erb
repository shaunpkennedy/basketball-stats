<div class="d-flex justify-content-between align-items-center">
    <h1>Games</h1>
</div>

<%= link_to "Back to Team", team_path(@team) %>

<%= link_to "Add a Game", new_team_game_path(@team), class: "btn btn-primary" %>

<% @games.each do |game| %>
  <p><%= game.name %></p>
<% end %>

